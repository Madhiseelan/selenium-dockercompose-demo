<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">

    <Properties>
        <!-- Folder will be auto-created if missing -->
        <Property name="logPath">logs</Property>
        <Property name="runId">${date:yyyy-MM-dd_HH-mm-ss}</Property>
        <Property name="layout">%d{yyyy-MM-dd HH:mm:ss} [%t] %-5p %c{1}:%L - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${layout}" />
        </Console>

        <RollingFile name="RollingFile"
                     fileName="${logPath}/app-${runId}.log"
                     filePattern="${logPath}/archive/app-%d{yyyy-MM-dd_HH-mm-ss}.log.gz">
            <PatternLayout pattern="${layout}" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10 MB" />
            </Policies>
            <DefaultRolloverStrategy max="20" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="INFO">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>

</Configuration>
